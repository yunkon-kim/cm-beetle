test:
  set:
    mode: parallel # parallel (per target) or sequential

  # Target CSP/region pairs. Recommendation does not provision anything, so enabling
  # several at once is cheap.
  cases:
    - csp: aws
      region: ap-northeast-2
      name: AWS-Seoul
      execute: true
    - csp: azure
      region: koreasouth
      name: Azure-Busan
      execute: false
    - csp: gcp
      region: asia-northeast3
      name: GCP-Seoul
      execute: false
    - csp: alibaba
      region: ap-northeast-2
      name: Alibaba-Seoul
      execute: false
    - csp: tencent
      region: ap-seoul
      name: Tencent-Seoul
      execute: false
    - csp: ibm
      region: au-syd
      name: IBMCloud-Sydney
      execute: false
    - csp: ncp
      region: kr
      name: NCP-Seoul
      execute: false
    - csp: nhn
      region: kr1
      name: NHNCloud-Pangyo
      execute: false

  # On-premise fixtures and what each must produce.
  #
  # expect.statusCode is required. The other expect fields are optional — omit one to skip
  # that check. Structural checks (non-empty name/version/specId, desiredNodeSize >= 1) always
  # run for statusCode 200.
  scenarios:
    - file: testconf/scenarios/honeybee-k8s-refined-infra.json
      name: baseline
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 2

    - file: testconf/scenarios/honeybee-k8s-workers0.json
      name: workers0
      execute: true
      expect:
        statusCode: 400 # control-plane only: recommendation cannot be formed

    - file: testconf/scenarios/honeybee-k8s-workers1.json
      name: workers1
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 1

    - file: testconf/scenarios/honeybee-k8s-workers5.json
      name: workers5
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 5

    - file: testconf/scenarios/honeybee-k8s-tiny.json
      name: tiny-upscale
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 1

    # Three distinct worker sizes (4/8, 8/16, 16/64). The two smaller ones consolidate — the
    # 4 vCPU worker sits at exactly 50% of the 8 vCPU spec — while 16/64 stays apart, since a
    # 4 vCPU worker would only use 25% of it.
    - file: testconf/scenarios/honeybee-k8s-3groups.json
      name: three-groups
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 2
        totalNodeSize: 3

    - file: testconf/scenarios/honeybee-k8s-hetero.json
      name: hetero-spec
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 2
        totalNodeSize: 2

    # Two source sizes that provably resolve to one target spec: both sit at or below the
    # minimum viable worker (2 vCPU / 4 GiB), so both searches run with identical clamped
    # bounds. Grouping on the source signature produced two node groups here.
    - file: testconf/scenarios/honeybee-k8s-converge.json
      name: converge-on-target-spec
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 2

    # Workers as honeybee reports them when it discovers nodes through the Kubernetes API:
    # cpus/cores are 0 and the total CPU count lands in threads. Same memory, different CPU
    # count, so these must stay apart — the old key computed cpus*threads and collapsed both
    # to one group sized from whichever worker came first.
    - file: testconf/scenarios/honeybee-k8s-k8sapi-cpus0.json
      name: k8s-api-collected-cpus0
      execute: true
      expect:
        statusCode: 200
        # Consolidated: 4 vCPU is 50% of the 8 vCPU spec and the memory matches exactly.
        # The workers still resolve to different specs first — that part is unchanged.
        nodeGroupCount: 1
        totalNodeSize: 2

    - file: testconf/scenarios/honeybee-k8s-mixed-arch.json
      name: mixed-arch
      execute: true
      expect:
        statusCode: 200
        totalNodeSize: 2

    - file: testconf/scenarios/honeybee-k8s-arm64.json
      name: arm64
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 2

    - file: testconf/scenarios/honeybee-k8s-samecpu-diffdisk.json
      name: samecpu-diffdisk
      execute: true
      expect:
        statusCode: 200
        totalNodeSize: 2

    - file: testconf/scenarios/honeybee-k8s-spec-small.json
      name: spec-small
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 2

    - file: testconf/scenarios/honeybee-k8s-spec-large.json
      name: spec-large
      execute: true
      expect:
        statusCode: 200
        nodeGroupCount: 1
        totalNodeSize: 2

    - file: testconf/scenarios/honeybee-k8s-v134.json
      name: version-1.34
      execute: true
      expect:
        statusCode: 200

    - file: testconf/scenarios/honeybee-k8s-v199.json
      name: version-1.99-fallback
      execute: true
      expect:
        statusCode: 200 # non-existent upstream version must fall back to the latest available

    - file: testconf/scenarios/honeybee-k8s-norole.json
      name: no-worker-role
      execute: true
      expect:
        statusCode: 400

    # Negative fixtures: cm-honeybee payloads captured before /infra/refined. Feeding these
    # directly is a realistic user mistake, and the API must reject them cleanly.
    - file: testconf/scenarios/negative/honeybee-k8s-raw-source-group.json
      name: neg-raw-source-group
      execute: true
      expect:
        statusCode: 400 # top-level JSON array: request binding fails

    - file: testconf/scenarios/negative/honeybee-k8s-raw-connection-info.json
      name: neg-raw-connection-info
      execute: true
      expect:
        statusCode: 400

    - file: testconf/scenarios/negative/beetle-k8s-recommendation-request.json
      name: neg-unwrapped-servers
      execute: true
      expect:
        statusCode: 400

beetle:
  endpoint: http://localhost:8056
  authConfigFile: testconf/auth-config.json
